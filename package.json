{
  "name": "world-cup-2026-bet-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "SQLite-backed World Cup 2026 bet tracker.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check admin.js && node --check scripts/import-initial-players.js"
  },
  "engines": {
    "node": ">=24"
  }
}
