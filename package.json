{
  "name": "world-cup-2026-bet-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "SQLite-backed FIFA World Cup 2026 prediction app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check admin.js && node --check scripts/import-initial-players.js && node --check scripts/probe-football-data.js"
  },
  "engines": {
    "node": ">=24"
  }
}
